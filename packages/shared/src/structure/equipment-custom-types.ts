/**
 * Common custom equipment types — quick-pick suggestions shown when adding a
 * "Custom" equipment node. Free-text entry is always allowed; the form also
 * merges in the custom types already used on the current project.
 */
export const COMMON_CUSTOM_EQUIPMENT_TYPES = [
  'UPS',
  'Lighting Control System',
  'Power Quality Meter',
  'Inverter',
  'Battery Bank',
  'Capacitor Bank',
] as const;
