# Claude Handoff — 2026-04-18

## 1. What was done this session

### UI Uplift — Win 1: Mobile token extraction + web palette alignment ✅
- Created `esite/apps/mobile/src/theme.ts` — full design-token system (`colors`, `spacing`, `radius`, `fontSize`, `fontWeight`, `fontFamily`, `priorityColor()`, `statusBadge()`)
- Mobile palette aligned to the web warm-dark palette (`#0D0B09` base, `#E8923A` amber) — same product feel across both surfaces
- All 20 mobile screens refactored: every hardcoded hex replaced with token imports. Only 1 intentional non-token value remains (`#F8F8F0` floor-plan canvas background)
- `app.config.ts` splash + adaptive-icon background updated to `#0D0B09`
- TypeScript type-check passed clean (0 errors)

### UI Uplift — Win 2: Web lucide-react icon swap ✅
- `lucide-react@0.441.0` was already installed — no new dep needed
- `Sidebar.tsx`: deleted 12 hand-rolled SVG icon functions, replaced with lucide imports (`LayoutGrid`, `FolderOpen`, `AlertTriangle`, `BookOpen`, `ShieldCheck`, `MessageSquare`, `ClipboardList`, `ShoppingBag`, `Users`, `CreditCard`, `Settings`, `LogOut`) via a one-liner `IC` props object
- `dashboard/page.tsx`: deleted 4 quick-action SVG functions, replaced with `FolderPlus`, `AlertTriangle`, `BookOpen`, `ShoppingBag`
- `NotificationCentre.tsx`: replaced inline bell SVG path with `<Bell size={20} />`
- **Type-check not yet confirmed for web** — tsc job timed out in background. Run `cd esite/apps/web && node node_modules/.bin/tsc --noEmit` at session start.

### Dev server / preview tooling
- `.claude/launch.json` updated: "Web (Next.js)" entry now uses `/opt/homebrew/bin/node` + absolute path to `next/dist/bin/next` (bypasses missing `pnpm` in sandbox PATH)
- `.claude/start-web.sh` helper script created (backup approach)
- **Known issue**: Claude's preview sandbox (`disclaimer` helper) blocks the node process from running — the server starts but hangs at 0 CPU. Workaround: run `cd esite/apps/web && node node_modules/next/dist/bin/next dev` in a real terminal manually, then verify visually in browser at `http://localhost:3000`

---

## 2. Current state

| Item | Status |
|---|---|
| Mobile theme tokens (`theme.ts`) | ✅ Shipped |
| All 20 mobile screens tokenised | ✅ Shipped |
| Mobile tsc clean | ✅ Confirmed |
| Web lucide icon swap (Sidebar, Dashboard, NotificationCentre) | ✅ Code done |
| Web tsc clean after icon swap | ⚠️ Not yet confirmed — do first |
| Win 3: Web `Form.tsx` wrapper | 🔲 Not started |
| Win 4: Skeleton / loading states (web + mobile) | 🔲 Not started |
| Win 5: Empty/error state standardisation | 🔲 Not started |

---

## 3. What's next (in order)

1. **Confirm web tsc is clean** after the lucide swap. Command: `cd esite/apps/web && node node_modules/.bin/tsc --noEmit`. If errors surface, fix before moving on.

2. **Win 3 — Web `Form.tsx` wrapper component.** Audit all web form pages (`/auth`, `/projects/new`, `/snags/new`, `/rfis/create`, etc.), extract a shared `<FormField label error>` wrapper that enforces: label typography, input border/focus ring using the amber token, error text style. Goal: one component, zero per-page form CSS drift.

3. **Win 4 — Skeleton loading states.** Web: `<Skeleton>` component using `animate-pulse` + the warm-dark tokens. Mobile: animated placeholder rows using `Animated.loop`. Roll out to the three highest-traffic list pages first (dashboard KPI cards, snags list, projects list).

---

## 4. Open questions / decisions needed

- **Preview server in Claude Code**: The sandbox blocks any node process spawned by `preview_start`. Options: (a) accept that UI verification must happen in a real browser, (b) investigate if there's a Claude Code setting to disable the sandbox for trusted local servers, (c) keep the `launch.json` entry as-is and just use it as documentation of the correct command. Recommend (a) for now.
- **`pnpm` not in PATH for Bash**: Confirmed pnpm is not installed system-wide (not in `/opt/homebrew/bin`, not in `~/.local/share`). All shell commands in this session use `node node_modules/.bin/...` directly. If you want `pnpm` available in Claude's Bash, install it: `npm install -g pnpm` or `brew install pnpm`.
- **Launch blockers are untouched**: T-020 Paystack pilot, staging migration deployment, and the duplicate `* 2.ts` file cleanup are all still open. The UI uplift is a parallel track — these need a separate focused session.

---

## 5. Context to load first (fresh session)

```
CLAUDE.md                                          # session bootstrap + next actions
SPEC DOCS/tasks.md                                 # 61 tasks, acceptance criteria
SPEC DOCS/spec-v2.md                               # product truth
esite/apps/mobile/src/theme.ts                     # mobile token system (core Win 1 output)
esite/apps/web/src/components/layout/Sidebar.tsx   # Win 2 output — lucide pattern to follow
esite/apps/web/src/components/ui/Badge.tsx         # existing web token patterns
esite/apps/web/src/app/globals.css                 # sidebar CSS, spacing scale
.claude/launch.json                                # corrected dev server command
```
